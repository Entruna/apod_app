class DateIsEmptyException implements Exception {
  final String message;

  DateIsEmptyException(this.message);
}

class ImageUrlIsEmptyException implements Exception {
  final String message;

  ImageUrlIsEmptyException(this.message);
}
